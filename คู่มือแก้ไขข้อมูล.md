# คู่มือแก้ไขข้อมูลเว็บภาควิชาจุลชีววิทยา

ข้อมูลทั้งหมดอยู่ในไฟล์ `Microbiology BUU.dc.html` แบ่งเป็นสองแบบ

**แบบที่ 1 — แก้ในหน้าเว็บได้เลย** (คลิกที่ข้อความในหน้าเว็บ)
ประกาศ/ข่าวสาร · ลิงก์แยกตามรหัสชั้นปี · ระบบมหาวิทยาลัย · เอกสาร/แบบฟอร์ม · เอกสารภายในหลักสูตร · PLO 1–7 · ข้อมูลติดต่อ

**แบบที่ 2 — แก้ในบล็อก `DATA`** (ต้นไฟล์ ระหว่างบรรทัด `DATA = {` ถึง `จบบล็อกข้อมูล`)
ปฏิทิน/การแจ้งเตือน · ตารางสอน · ตารางสอบ · แผนการเรียน · Curriculum mapping · รายละเอียดรายวิชา
ส่วนเหล่านี้ต้องคำนวณ (นับวันถอยหลัง จัดกลุ่มชั้นปี กรองรายวิชา) จึงเก็บเป็นข้อมูลแยก

---

## 1. เพิ่มวัน–เวลา ให้รายวิชา

ใน `DATA.courses` แต่ละบรรทัดหนึ่งวิชาหนึ่งกิจกรรม

```js
{ code: '30520664', name: { th: 'Determinative Bacteriology (บรรยาย)', en: '…' },
  years: [2], kind: 'lecture', room: 'BS4203', day: 'จันทร์', time: '09.00–12.00' },
```

| ช่อง | ความหมาย |
| --- | --- |
| `code` | รหัสวิชา |
| `name` | ชื่อวิชา (`th` ภาษาไทย, `en` ภาษาอังกฤษ) |
| `years` | ชั้นปีที่เรียน เช่น `[2]` หรือ `[3, 4]` ถ้าเรียนร่วม |
| `kind` | `'lecture'` = บรรยาย, `'lab'` = ปฏิบัติการ |
| `room` | ห้อง ถ้าเว้น `''` หรือ `'—'` เว็บจะแสดง "ไม่ระบุห้อง" |
| `day` | วัน เช่น `'จันทร์'` เว้น `''` ได้ |
| `time` | เวลา เช่น `'13.00–16.00'` เว้น `''` ได้ |

วันและเวลาจะขึ้นเป็นป้ายบนการ์ดรายวิชาในหน้าตารางสอน ถ้าเว้นทั้งสองช่อง ป้ายจะไม่ขึ้น

## 2. เพิ่มรายการปฏิทินและการแจ้งเตือน

ใน `DATA.calendar` ใช้วันที่แบบ `'YYYY-MM-DD'` เป็น พ.ศ. ลบ 543 (7 ก.ย. 2569 → `'2026-09-07'`)

```js
{ start: '2026-10-15', title: { th: 'ส่งข้อสอบปลายภาค', en: 'Submit final exam papers' }, kind: 'exam' },
{ start: '2026-11-02', end: '2026-11-06', title: { th: 'สอบปลายภาค', en: '…' }, kind: 'exam' },
```

`end` ใส่เมื่อเป็นช่วงหลายวัน · หน้าต่างแจ้งเตือนจะขึ้นเองเมื่อเหลือไม่เกิน 7 วัน (แก้จำนวนวันที่ `DATA.reminderDays`)

## 3. เพิ่มรายละเอียดรายวิชา (หน้ารายละเอียด)

ใน `DATA.courseDetails` ใช้รหัสวิชาเป็นคีย์

```js
courseDetails: {
  '30510469': {
    credits: '3(3-0-6)',
    description: { th: 'คำอธิบายรายวิชา…', en: '' },
    owners: ['ผศ.ดร. …', 'อ. …'],
    plos: [2, 3, 7],
    materials: [ { name: { th: 'เอกสารบทที่ 1', en: '' }, href: 'uploads/ไฟล์.pdf' } ]
  }
}
```

## 4. ขึ้นเทอมใหม่ / ปีการศึกษาใหม่

ทำ 5 ขั้น ใช้เวลาไม่นานเพราะรูปแบบข้อมูลเหมือนเดิมทุกเทอม

1. **สำรองของเดิม** คัดลอกไฟล์เป็น `Microbiology BUU 1-2569.dc.html` เก็บไว้เป็นข้อมูลย้อนหลัง
2. **แก้หัวเทอม** `DATA.term` และ `DATA.updated`
3. **อัปโหลดไฟล์ PDF ใหม่** แล้วแก้ `DATA.teachPdf` / `DATA.examPdf` ให้ชี้ไฟล์ใหม่ (และแก้ลิงก์สองรายการท้ายหน้าเอกสาร)
4. **ตารางสอน** แก้ `DATA.courses` — วิชาที่เปิดซ้ำทุกปีแก้แค่ `day` / `time` / `room`
5. **ตารางสอบ** ใน `DATA.exams` แก้ `days` (วัน–วันที่หัวคอลัมน์) แล้วแก้ช่องสอบ

   หนึ่งช่อง = หนึ่งวันของชั้นปีนั้น หลายวิชาในวันเดียวคั่นด้วย `;`
   หนึ่งวิชา = `'เวลา|รหัส ชื่อวิชา|ห้อง ผู้คุมสอบ'`

   ```js
   { year: 'ปี 2', cells: [
     '09.00–12.00|31220164 สถิติฯ|',                 // จันทร์
     '',                                              // อังคาร — ไม่มีสอบ
     '09.00–12.00|30531364 Microbial Genetics|QS1-4001 พัชรนันท์',
     '08.00–10.00|30520664 Deter Bac|QS1-4001 สุดสายชล;13.00–16.00|89520864 ทักษะภาษาไทยฯ|',
     ''
   ] },
   ```

   จำนวนช่องใน `cells` ต้องเท่ากับจำนวนวันใน `days` ของตารางนั้น

6. **ปฏิทิน** ล้าง `DATA.calendar` ของเทอมเก่าแล้วใส่วันสอบและกำหนดส่งของเทอมใหม่

ถ้าอยากให้ผมทำให้ ส่งไฟล์ตารางเรียน–ตารางสอบของเทอมใหม่มาได้เลย
